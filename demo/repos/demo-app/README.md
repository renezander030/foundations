# demo-app

A tiny demo project so the `repos` (files) source has something to find.

It contains a small search module that uses a vector index and a cache. Present
in the tree does not mean it runs in your environment — which is exactly why the
`repos` source is tagged `code` (unverified), not proven.
